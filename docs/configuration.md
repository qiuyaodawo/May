# Configuration reference moved

The canonical configuration reference now lives at
[`reference/configuration.md`](reference/configuration.md).

This compatibility page is retained so existing links do not break.