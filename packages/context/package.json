{
  "name": "@may/context",
  "version": "0.1.0",
  "description": "Replaceable context factories and default context implementations for May agents.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./model-summarizer": {
      "types": "./dist/model-summarizer.d.ts",
      "import": "./dist/model-summarizer.js"
    }
  },
  "files": [
    "dist",
    "!dist/.tsbuildinfo"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean",
    "test": "pnpm run build && node --test test/*.test.mjs",
    "test:coverage": "pnpm run build && node --test --experimental-test-coverage test/*.test.mjs"
  },
  "dependencies": {
    "@may/core": "workspace:*"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
