{
  "name": "@may/providers",
  "version": "0.1.0",
  "description": "Provider selection, registration, and built-in model composition for May.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist", "!dist/.tsbuildinfo"],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean",
    "test": "pnpm run build && node --test test/*.test.mjs",
    "test:coverage": "pnpm run build && node --test --experimental-test-coverage test/*.test.mjs"
  },
  "dependencies": {
    "@may/config": "workspace:*",
    "@may/core": "workspace:*",
    "@may/provider-anthropic": "workspace:*",
    "@may/provider-deepseek": "workspace:*",
    "@may/provider-kimi": "workspace:*",
    "@may/provider-openai": "workspace:*",
    "@may/provider-openai-compatible": "workspace:*",
    "@may/provider-zhipu": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0"
  },
  "engines": { "node": ">=20" },
  "license": "MIT"
}
