{
  "name": "@may/provider-zhipu",
  "version": "0.1.0",
  "description": "Zhipu GLM model adapter for May.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "!dist/.tsbuildinfo"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean",
    "test": "pnpm run build && node --test test/*.test.mjs",
    "test:coverage": "pnpm run build && node --test --experimental-test-coverage test/*.test.mjs",
    "test:integration": "pnpm --filter @may/config run build && pnpm run build && node --test test-integration/*.test.mjs"
  },
  "dependencies": {
    "@may/core": "workspace:*",
    "@may/provider-openai-compatible": "workspace:*"
  },
  "devDependencies": {
    "@may/config": "workspace:*"
  },
  "engines": {
    "node": ">=22"
  },
  "license": "MIT"
}
